# 04: Live PGSR A/B on one capture

**What to build:** one pinned PGSR training plus both extractions from that same training, judged against the two existing routes on the same ruler.

**Answers:** M8

**Blocked by:** 02 pinned source record, 03 cheap boxes first.

**Status:** ready-for-agent

- [ ] Single training at full capture density with no silent downsample, both variants extracted from it: fusion-masked via the community alpha path, and cull-after on stock with a small-component filter only
- [ ] Every extraction states voxel size and truncation band in millimetres; block counts and free-memory figures printed before any extraction call
- [ ] Cross-view depth disagreement in millimetres for PGSR versus the MILo route on the same capture and ruler; same-ruler photogrammetry comparison as the shared figure
- [ ] Break-face close-ups for both variants versus MILo at a view resolving roughly-two-tenths-of-a-millimetre ridges exist before any score; whole-tray views alone do not pass
- [ ] Clamp-contact faces recorded as unobserved, never filled; no batch submission without explicit approval

## Comments

- 2026-09-10: env delta complete. `diff-plane-rasterization` wheel-built (job 30296655; its FAILED state was a CWD-shadowing check bug, verified fine). `pytorch3d` source-built at pinned `v0.7.7`, sm_80 only, 3.5 min (job 30385970, `import ok: 0.7.7`). Shared `envs/milo` otherwise untouched — still reuse, no fresh env.
- 2026-09-10: train attempt 30387871 failed in 19 s on layout, not code — stock PGSR reads a FLAT `sparse/` (`images.bin`, line 150) while the dataset keeps COLMAP's `sparse/0/` project layout; the bare `except` then fell through to a missing text model. Fixed with per-file symlinks into a flat `sparse/` (no stock change, pin clean) and resubmitted as 30390922. Guard updated to check the flat path so this class fails on the laptop side next time.
- 2026-09-11: crop test refused twice, honestly. Both pre-fix meshes (A fusion-masked, B cull-after) land **0% inside** the OpenMVS fixed-frame reference box (`crop_mesh.py` refused rather than returning empty — the tool working as designed). Centroids ~5 units off with 2–5× spans → wrong frame, not empty object.
- 2026-09-11: frame root cause found by probe (`scripts/probe_fusion_pose.py`, committed): stock `render.py` hands Open3D a world-to-camera matrix where it takes camera-to-world, so every view fuses through an inverted pose (0/61590 points forward under the assumed camera vs 85.1% under COLMAP control). One-line fork invert recorded as fork change #1 (training untouched — rasterizer path self-consistent). Re-fused A2 still 0% in box with all 10 clusters ~5–7 units out at ~max-depth range → the pose was real but not sufficient; training itself was warped (see next line). Pre-fix A/B/A2 meshes are INVALID superseded mush, never to be compared or scored.
- 2026-09-11: training-frame verdict by held-out views. Full-regs 30k: **15.5 dB** mean over 21 held-out views, cloud spans 4–5× the scene in Y/Z — training smeared, both A/B meshes inherited it (mush, not a reconstruction; the earlier crop/centroid puzzle dissolves). Probe (7k, all three geometric reg weights zeroed, fresh dir): **22.9 dB**, rig crisply visible in renders. One variable differs → the planar single-view + multi-view NCC/geometric losses fight this material (thin specular rods, textureless black background). Full 30k regs-off submitted as batch 30424099 into output/A03_noreg; exposure stays off for isolation (follow-up arm if it plateaus). Both variants will extract from that model.
- 2026-09-11: proposed reason for the smear (researched: paper §§ single/multi-view reg + stated reflective-surface limitation; pinned loss code; 3DReflecNet/NeRO/Ref-NeuS on consistency-vs-specular). The three losses bet matte + textured + stable-light; the rig is chrome (highlights move → NCC patches systematically disagree, Pho term stuck ~0.04 all run), the backdrop is textureless black (single-view weight (1−grad)² enforces hardest where no surface exists), and exposure drifts with compensation OFF (paper's own switch for this). Decisive test: the regs-on run saved its 7k checkpoint, one iter BEFORE reg weights engage at 7000 — render held-out views from it; ~22 dB says the collapse starts with the losses (causal), already-smeared says the proposal is wrong. Test running on a fresh holder via scripts/gpu_session.sh.
- 2026-09-11: test CONFIRMED the proposal. 7k checkpoint (regs about to engage): **22.9 dB** — identical to the regs-off probe, same 21 views. Final 30k (regs active throughout): **15.5 dB**. Same data, same code, same run; the only change in the 7k→30k window is the reg weights switching on. The collapse starts with the losses. Regs-off 30k (batch 30424099) is therefore the verdict run; its mesh gets drawn before anything is read off it.
- 2026-09-12: regs-off 30k COMPLETED in 3:33, exit 0 (output/A03_noreg, 7k + 30k checkpoints). Both variants extract from the 30k model next. Process note: helper holder 30425702 timed out untouched — requested before the blocking-start fix, watch died in a server restart, grant unnoticed. Fresh holder requested via the fixed helper.
- 2026-09-11: user-proposed max_depth 4.0 cutoff checked and DECLINED with evidence. Per-view max depth of box content: mean 4.31, max 4.81; 163/164 views hold content past 4.0 (cameras orbit at ~3.77 but look through the ~1.6-wide tray to its far side). A 4.0 cutoff would carve the far side in nearly every view. Default 5.0 already excludes the room and keeps all box content (margin 0.2 units). Keep 5.0 for the regs-off extractions.
- 2026-09-13: POSE VERDICT — fork change #1 (inv(pose) before integrate) WITHDRAWN, was backwards. Chain: depth-histogram probe (`PGSR/scripts/probe_depth_hist.py`, holder 30472519) acquitted the depths (8 views: medians 3.4–3.9 ≈ orbit, 0% piled at max); back-projection probe (`probe_backproject.py`) showed inv(pose)-as-C2W beats pose 0.10 vs 6.87 meanNN — but that tests pure projection, not the integrator. Fresh masked fusion with the fork lands bit-identical displaced ([-0.60,1.19,4.28].., 134207 verts); fused stock it sits on the training points ([-0.74,-0.33,-0.44].. vs points [-1.01,-0.31,-0.42].., 96719 verts). Upstream W2C pass-through was correct (its 0.47 DTU chamfer said so); the 09-11 probe only proved pose ≠ inv(pose), never which the integrator wants. `mesh_A_masked` (Sep 12, fork-posed) is INVALID displaced; the stock-posed mesh is under review at `PGSR/artifacts/review_A_stock/`. Fork #1 reverted in `render.py`, guard flipped, `PGSR/AGENTS.md` record struck through. B's OOMs stand as the real content×voxel ceiling.
