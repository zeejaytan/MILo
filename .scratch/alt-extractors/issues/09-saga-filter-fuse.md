# 09: SAGA post-hoc filter then fuse

**What to build:** freeze the full-scene MILo Gaussians, distil SAM masks to per-Gaussian features, filter to the sherd subset at full density, then reuse the live fusion-time masked-depth path — steel out without thinning clay.

**Answers:** M7

**Blocked by:** 01 (OpenMVS on A03, same ruler), 02 (depth disagreement in mm).

**Status:** ready-for-agent

**Pinned (2026-09-06 — code audited at pin, laptop clone):** `Jumpat/SegAnyGAussians@v2@2d4c5d7` (verified HEAD on clone; full SHA `2d4c5d77c857c956d747e4775d3d72c4ec5dfe16`). Cloning to `/data/gpfs/projects/punim2657/saga/` + env `envs/saga` build running on login node (background 2026-09-06).

**Audit verdict, 2026-09-06 (desk, no GPU) — GO with one known risk:**
1. Geometry compatible: `GaussianModel.load_ply` reads by attribute NAME (`x,y,z,opacity,f_dc_0..2,f_rest_*` with count asserted `3*(sh+1)^2-3`); MILo's 19 extra props (`filter_3D`, `base_occupancy_*`, `occupancy_shift_*`) are ignored, f_rest 45 = sh3 asserts clean. Frozen source: `output/17062025/A03_nomask/point_cloud/iteration_18000/point_cloud.ply` (222,678 Gaussians — the M5 control run).
2. SAM extraction SKIPPED by design: SAGA's `extract_segment_everything_masks` + `get_scale` replaced by OUR adapter-verified masks — `sam_masks/<stem>.pt` converted from `images_masked` alpha (164/164 views, sherd-only erode0). Single binary mask/view is sufficient: contrastive loss then learns sherd-vs-background, exactly the filter wanted. Scales via SAGA's own `get_scale.py` on the staged scene.
3. Query headless: notebook path (`render features → query pixels → cosine sim → segment(>0.75) → .pt`) scriptable with our masks as the prompt — no GUI/notebook needed.
4. KNOWN RISK, update 2026-09-07: confirmed — no pytorch3d wheel for (py311, torch 2.3, cu118) at either per-combo page; login-node compile of simple-knn + all three rasterizer forks clean. pytorch3d v0.7.6 (torch-2.3 era) building from source on a GPU node via `slurm/saga_pytorch3d.slurm`, knn smoke-tested on GPU in-job.

- [x] SAGA feature head trained on frozen full-scene `A03_nomask` Gaussians (222,678) at full capture resolution with OUR masks as the stack (`sam_masks/*.pt` from `images_masked` alpha; scales via `get_scale.py`; `train_contrastive_feature.py`) — job 30196905 COMPLETED 0:0 2026-09-07 (loss 0.05, 2D pos 0.86 / neg −0.55); `contrastive_feature_point_cloud.ply` + `scale_gate.pt` saved at iter 10000
- [ ] Sherd subset retrieved by headless mask-prompt query, keep-index → filtered ply → live path `mesh_extract_dtu.py:133-135` + `integration.py:54-59`, voxel size and truncation band stated in mm
- [ ] Same-ruler scoring vs OpenMVS (fraction within ~1 mm), break-face close-ups resolving ~0.2 mm ridges, remaining steel in cm² (`mask_content.py` split — rig was 644 cm² vs 61 cm² clay per view)
- [ ] If filtering webs steel or drains clay like M4/M5: retire NO at one capture with eye verification (type-1 method failure), do not fund a second post-hoc architecture

## Comments

- 2026-09-06 (trail start, both-in-order track 2/3): M7 added this track 2026-09-06 but spec/issues 01–08 never ticketed it — this file closes the gap. Caution: SAGA paper notes weak regime on small targets absent from SAM masks (ours 2.28% of frame); masks must be consistent across all 143 views or the ID splits. Geometry untouched by construction (only affinity dim-32 attached). Standing approval for Slurm submits noted 2026-09-06.
- 2026-09-07: env complete (torch 2.3.1+cu118; simple-knn + 3 rasterizers compiled login-node; pytorch3d 0.7.6 source-built on GPU, knn GPU-tested). Submitted job 30169355 (`slurm/saga_train.slurm`): stage our 164 masks to `sam_masks/*.pt`, `get_scale.py`, then feature-head-only 10k iters on frozen A03_nomask scene. Query + fusion in a second job after looking. Poll running.
- 2026-09-07, 30169355 FAILED in setup (`matplotlib` missing — `get_scale.py` imports it at top; masks converted fine, 164/164). Installed on login node, resubmitted as job 30169660, poll running.
- 2026-09-07, 30169660 FAILED in setup again: fresh model dir has no `cfg_args` and SAGA's loader only catches TypeError, not the missing file (upstream never sees this — scene training always writes it first; we skip that step by design). Seeded both configs with the loader's own empty default (`Namespace()` — merge becomes cmdline-only, nothing invented). Resubmitted as job 30169764, poll running.
- 2026-09-07, 30169764 FAILED in setup a third way: the empty seed broke extraction the other direction (`dataset.sh_degree` missing — the seed must carry real defaults, not emptiness). Replaced with seeds built from SAGA's own param classes (verified laptop-side against the pin: eval round-trips, sh_degree 3, paths land). Resubmitted as job 30169966, poll running.
- 2026-09-07, 30169966 FAILED at the method boundary, first real signal: single-mask stack collapsed under get_scale's `.squeeze()` (`[1,H,W]`→`[H,W]`, indexed a row — latent upstream bug, SAM always returns N>1). Fixed in OUR data, not their code: two-stack `[sherd, background]`, which the contrastive loss also needs (push needs a second class). Resubmitted as job 30170352, poll running.
- 2026-09-07, 30170352 FAILED past staging (scales computed): training asserts a ray count the class defaults leave at 0 — upstream's own recipe passes it on the flag (`train_spin.sh`: 1000 rays on ~1MP views). Passed 8000, scaled for 6.8MP full-res at 2.3% coverage (~180 clay rays/view/iter). Resubmitted as job 30185463, poll running.
- 2026-09-07 queue detour (recorded in `docs/lessons.md`): 128G mem silently reroutes jobs to gpu-a100 regardless of partition flag (bisected: 64G stays short). Train now 64G on short as job 30196905 (poll running); held session 30196960 on short/8CPU/2h, waiter running — iteration moves inside it once RUNNING.
- 2026-09-07 15:49 UTC: 30196905 RUNNING — first real training: frozen scene loaded at iter 18000, 164/164 cameras read, adaptive scale gate on, quantile fitted on 328 samples (164 views × our 2-stack — the masks are flowing through). Session still queued behind it.
- 2026-09-07: 30196905 COMPLETED 0:0 (loss 0.05) — features saved. Query job 30204989 submitted (`slurm/saga_query.slurm`, GPU leg only; primary threshold 0.6 prespecified; overlays for the eye before any fusion). Session 30196960 never started (GONE from queue) — batch legs carry the work; held session retried only if iteration stalls. Poll running on the query.
- 2026-09-07, 30204989 FAILED in seconds (exit 2): my flags leaked into SAGA's argv parser. Hidden behind `sys.argv[:1]` — saved cfg carries all SAGA values. Resubmitted as job 30206020 (short, verified), poll running.
- 2026-09-07, 30206020 FAILED in seconds: bare argv left model_path empty so the cfg lookup failed. Rebuilt argv with SAGA-known flags carrying our paths. Resubmitted as job 30208471 (short, verified), poll running.
- 2026-09-08, 30208471 ran the query core then died on overlays (`image_name` is the stem — `A31_1100`, no extension). Core numbers before the crash need a hard look: kept 5,272 @0.5 / 2,778 @0.6 / 485 @0.75 of 222,678 (2.4% / 1.2% / 0.2%) — 6.6× fewer than even the retired-too-coarse M5 result at primary. Either the threshold sits above the true separation or the filter starves clay. Distribution + overlays decide; nothing fuses until they do. Fixed stem→glob, resubmitted as job 30233722 (short, verified), poll running.
- 2026-09-08, distribution (login-node CPU, `scripts/saga_sim_ladder.py`): single hump at −0.2, thin positive tail — NO bimodal gap, every threshold a trade. Overlays at 0.6 keep sherd fragments only (no rig — precision good) but most sherds dark: prespecified 0.6 was miscalibrated (anchored on 2D training numbers; 3D point sims live lower). Overlay ladder {0.0,0.1,0.2,0.6} decides whether lower thresholds recover all sherds without the rig. Resubmitted as job 30234092 (short, verified), poll running.
- 2026-09-08, ladder says 0.1: overlays keep every visible sherd on all 3 views (0.1: 24k kept; 0.2: 16k; 0.0 floods background). Minor background patches at 0.1 die in fusion masks anyway. Primary moved 0.6→0.1 ON THIS EVIDENCE (0.2 sensitivity). Fusion job 30234284 (`slurm/saga_fuse.slurm`: filter→DTU rung 0 at 0.822 mm, no cull, scale+report; renders+OpenMVS compare in verdict step), short/verified, poll running.
- 2026-09-08, fusion COMPLETED 0:0 both thresholds: 0.1 → 24,292 kept (footprint 1.35 mm), grid 19k blocks (under cliff, CPU binary, 0 non-finite); 0.2 → 16,229 (0.97 mm). Meshes scaled with sidecars pending. Verdict job 30234537 (`slurm/saga_verdict.slurm`: sidecar on same ruler, whole-mesh renders both routes, same-ruler OpenMVS compare), short/verified, poll running. Steel evidence: all ten pieces clay-positive redness (+7..+28, steel ≈ −5).
- 2026-09-08, 30234537 renders done but compare REFUSED (rightly): sidecar written as `<mesh>.ply.scale.json`, code wants stem form. One-word fix, resubmitted as job 30234685 (already RUNNING on short), poll running.
- 2026-09-08, 30234685 passed the scale gate then died on `--capture` (wants the JSON file, not the folder). Fixed, resubmitted as job 30234797.
- 2026-09-08, 30234797 OOM-killed at 67G on a 64G job (ticket-01's baseline compare peaked at 24G — ours allocates more somewhere unprofiled). Stopped squeezing: verdict mem to 128G, which routes to gpu-a100 by the documented mechanism — same as the house compare script. Resubmitted as job 30236662 (a100, verified), poll running.
- 2026-09-08, 30236662 OOM-killed at 131G in 5 min: the 164-view full-res render genuinely needs >128G; the 21-view held-out instrument did the same pair class at 24G (job 30158911). Root cause found, not worked around: verdict now scores the 21 sherd-only held-out views (the honest instrument anyway) via `data/17062025/A03/capture_sherds.json`, back to 64G/short. Resubmitted as job 30238171 (short, verified), poll running.
- 2026-09-07 queue reality: `gpu-a100` 203 pending — both the train (30185463) and the held session (30189862) sat queued with zero progress. Moved both to `gpu-a100-short` (33 pending / 8 running): train resubmitted as job 30196476 with `--time 2:00:00` override (10k iters take minutes; 4h was slack), session restarting at 2h. Old jobs scancelled before starting — no GPU burned.
